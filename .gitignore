# Foundry build artifacts
cache/
out/

# Broadcast logs (can include local keys if misconfigured)
broadcast/

# Secrets — never track env files or private keys
.env
**/.env
.env.*
!.env.example
!**/.env.example
*.key
**/*.key
# Runtime key material lives outside this repo (~/.gas-rescue/). Never copy it in.

# Coverage
lcov.info
coverage/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
